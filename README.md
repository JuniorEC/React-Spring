# Como usar:

## Spring-boot
Rode os comandos :
        > cd backend
        > ./gradlew build
        > ./gradlew bootRun

## React app
Rode os comandos:
        > cd frontend
        > npm install
        > npm start
