# Como usar:

## Spring-boot
1. Rode os comandos:

        > cd backend
        > ./gradlew build
        > ./gradlew bootRun

## React app
2. Rode os comandos:

        > cd frontend
        > npm install
        > npm start
